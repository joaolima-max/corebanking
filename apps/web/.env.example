# URL base da API backend (sem trailing slash)
# Em produção: URL do Railway, ex: https://bass-api.up.railway.app
# Em desenvolvimento: deixar vazio (proxy do Next.js) ou http://localhost:3000
NEXT_PUBLIC_API_URL=""

# Ambiente exibido na TopBar (STAGING, PRODUCTION, DEV)
NEXT_PUBLIC_ENV="STAGING"
